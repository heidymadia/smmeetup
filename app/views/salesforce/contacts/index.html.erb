<h1>Listing contacts</h1>

<table>
  <tr>
    <th>First name</th>
    <th>Last name</th>
    <th>Phone</th>
    <th>Description</th>
  </tr>

<% @salesforce_contacts.each do |contact| %>
  <tr>
    <td><%=h contact.first_name %></td>
    <td><%=h contact.last_name %></td>
    <td><%=h contact.phone %></td>
    <td><%=h contact.description %></td>
    <td><%= link_to 'Show', contact %></td>
    <td><%= link_to 'Edit', edit_salesforce_contact_path(contact) %></td>
    <td><%= link_to 'Destroy', contact, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New contact', new_salesforce_contact_path %>
